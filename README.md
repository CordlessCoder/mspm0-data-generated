# mspm0-data generated output

This repo contains generated output for [`mspm0-data`](https://github.com/CordlessCoder/mspm0-data). It is updated for every push to the `master` branch. See the `mspm0-data` README for more details.
